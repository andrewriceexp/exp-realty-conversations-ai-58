
// Re-export from the hooks implementation
import {
  useToast,
  toast,
  ToastProvider,
  type Toast,
  type ToastProps,
  type ToastActionElement,
} from "@/hooks/use-toast";

export {
  useToast,
  toast,
  ToastProvider,
  type Toast,
  type ToastProps,
  type ToastActionElement,
};
